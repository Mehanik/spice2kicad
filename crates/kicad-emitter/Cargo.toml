[package]
name = "kicad-emitter"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Emit KiCad schematic and netlist S-expressions from a typed circuit model"

[lints]
workspace = true

[dependencies]
spice-parser.workspace = true
spice-layout.workspace = true
spice-route.workspace = true
kicad-symbols.workspace = true
lexpr.workspace = true
thiserror.workspace = true
log.workspace = true
uuid.workspace = true

[dev-dependencies]
spice-diagnostics.workspace = true
# Phase-4.5 severed-net guard regression test (`src/refine.rs` tests):
# it needs a REAL fixture placement, so it runs the same
# parse → resolve → policy → place chain the CLI does. No cycle —
# none of these depend on `kicad-emitter`.
spice-resolve.workspace = true
spice-policy.workspace = true
